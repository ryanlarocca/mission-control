# Example env vars. Real values live in .env.local (gitignored).
# This file documents variable NAMES only — never commit secrets.

# --- Physiq project (gym tracker) ---
# Separate Supabase project from the LRG Homes CRMS one. See lib/physiqSupabase.ts.
NEXT_PUBLIC_PHYSIQ_SUPABASE_URL=
NEXT_PUBLIC_PHYSIQ_SUPABASE_ANON_KEY=
PHYSIQ_SUPABASE_SERVICE_ROLE_KEY=
# Management API PAT + project ref — used by scripts/seed-gym-tracker.mjs and migrations.
PHYSIQ_SUPABASE_PAT=
PHYSIQ_SUPABASE_PROJECT_REF=
